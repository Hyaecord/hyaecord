Hyaecord — Build Prompt (for Claude Code)


This document is meant to be handed to Claude Code as a working brief. Decisions that were open in earlier drafts have been resolved where possible and marked below. Where something is still genuinely undecided, it's flagged [OPEN] — stop and ask rather than guessing.

How to use this doc

Build V1 scope first, in the order it's listed. Treat V2 / Later as backlog — don't implement unless explicitly asked to.
Anything marked [OPEN] is a real decision point, not a detail to improvise. Ask before building it.
Anything marked ⚠ verify is a technical claim (API shape, CLI syntax, platform behavior) that needs to be checked against current docs before being relied on in code — it may be stale or approximate.
Everything here is additive on top of standard Discord functionality (see the Feature Parity Checklist) — no core Discord feature should be lost while building the custom stuff.

Naming — resolved

Product name: Hyaecord (hyena family). This replaces the earlier placeholder "Felicord" everywhere — repo name, window title, .desktop file, package names, tray tooltip, first-run wizard copy, etc. If any scaffolding was already generated under the old name, it needs a global rename pass, not just a display-string swap (check package.json name, build config app IDs, icon filenames, and any hardcoded strings before assuming a find-and-replace caught everything).

This unblocks part of one previously-open item below (Flatpak app ID) but not all of it — see that entry.

Mascot design is still separate and still open (see below). A hyena mascot is a natural direction given the name, but that's a call for whoever's doing the visual design, not something to bake into the build.

Confirmed Tech Direction

Framework: Electron. **Hyaecord is a complete overhaul, not a literal fork of Equicord's codebase.** The client shell, GUI, resource management, and desktop integration are original work built from scratch. The one piece that deliberately mirrors Equicord is the plugin system: Hyaecord implements its own compatibility layer against the Equicord/Vencord plugin API so that **most** existing plugins run without modification. Full, guaranteed 1:1 compatibility with every plugin in both ecosystems is *not* a hard requirement — plugins that reach into Equicord/Vencord-specific internals, private APIs, or DOM structure that Hyaecord's original GUI doesn't replicate may need porting or may simply not work, and that's an acceptable tradeoff for the rebuild. Rationale for keeping the plugin API compatible at all: the existing Equicord/Vencord plugin ecosystem is large, and rebuilding that ecosystem from zero would be a much bigger undertaking than building a compatibility layer against an existing, well-understood plugin API.

Because the GUI is original rather than a themed pass over Equicord's DOM, expect a nonzero set of plugins — especially ones that patch specific UI components or rely on Equicord/Discord's stock class names and component tree — to need adaptation. Track this as a real category of work (a compatibility matrix / known-issues list), not an edge case to ignore.

Platforms:

Linux — primary, v1 target
Windows — planned, build OS-detection generically now so this doesn't require re-architecting later
macOS — not planned

System tray / taskbar icon: must show Hyaecord's actual branded icon (proper multi-resolution .ico/.png/.svg icon set per platform) from day one — not a placeholder or the default Electron window icon. This needs real icon assets wired into the packaging step early, not bolted on right before release. Icon assets need to exist under the new name before this ships — don't package placeholder or leftover Felicord-named files.

V1 Scope — build this first

Plugin compatibility layer — supports most Equicord (and by extension Vencord) plugins unmodified via a compatibility layer against the plugin API, not via reuse of Equicord's client code; Equicord Cloud Saves for settings sync. Maintain a known-issues/compatibility list for plugins that don't work out of the box.
Built-in integrations (not plugins): UserPFP, UsrBG, GlobalBadges, RPC Bridge. On top of the badges GlobalBadges fetches (Vencord/Equicord contributor badges etc.), Hyaecord adds its own **"Hyaecord Contributor"** badge — icon is simply the Hyaecord logo, holders sourced from GitHub contributors of the org's repos (defined in `src/shared/badges.ts`).
Custom GUI base — server list → channel list → chat → member list, familiar structure, fully redesigned visuals and original DOM/component structure (not a CSS reskin of Discord's or Equicord's DOM). Native-OS-adjacent visual direction: lean on GTK4/Libadwaita and KDE Plasma for spacing/controls/system feel, but give Hyaecord-specific surfaces their own distinct identity.
Platform-native theming — auto-detect GNOME/KDE on first launch, default to matching theme, manual override in settings, graceful fallback elsewhere.
Built-in default themes — Light, Dark, AMOLED, meeting WCAG AA contrast as the baseline bar.
Server banner/icon rendering — give Discord's already-high-res uploads more visual room instead of the cropped stock display. ⚠ verify before hardcoding: current public write-ups of Discord's upload specs are inconsistent with each other (server banner upload target is variously reported as 960×540 vs. 1920×1080; free-tier file size cap is reported anywhere from 8MB to 10MB depending on source and date). Don't trust a blog post here — pull the actual behavior from Discord's own developer docs or by testing an upload directly, and record what you find so it doesn't need re-deriving later.
Performance core — fast startup, event-driven resource management (not polling), Gaming Mode (event-driven detection, focus-aware behavior across monitors, minimal always-on listener for mentions/DMs/calls).
Accessibility baseline — screen reader semantics/ARIA, full keyboard navigation, reduced-motion setting (respecting OS-level pref + in-app override), independent text/UI scaling, non-color-dependent notification cues.
Frictionless setup — Flatpak/AppImage/deb/rpm/AUR, no terminal required; first-run wizard (DE detection → theme, import existing Discord/Vesktop/Equibop login, plain-language explanation of opt-in features).
Feature parity checklist — verify nothing core is lost: search, pins, history, calls/screen share/go-live, spellcheck + non-Latin input, emoji/GIF/sticker picker, native notifications, tray/dock integration, multi-account, keybinds, auto-update, push-to-talk + device switching unaffected by Gaming Mode.
Legal/privacy/security basics — plain ToS disclosure (client mods violate Discord ToS; no observed ban pattern for mod use — ⚠ verify this is still accurate before publishing), trademark disclaimer, basic plugin sandboxing/permission boundaries where feasible.
Telemetry — **owner decision (22 July 2026): anonymous opt-out telemetry, on by default.** A once-daily ping (random UUID, app version, OS, arch, DE family — nothing else, never linked to a Discord account) answers "how many users, on what platforms". Custom events may be added via `recordEvent()` in `src/main/telemetry.ts` but must stay free of message content, identifiers, and per-user profiling, and are gated by the same single opt-out. The opt-out is disclosed in the first-run wizard and lives in Settings; off means off (no goodbye ping). Endpoint: `https://hyaecord.vercel.app/api/telemetry`. [OPEN] storage backend for aggregation (Vercel KV vs. Cloudflare D1 vs. self-hosted) — the endpoint currently validates and discards.
Motion/animation system — one shared set of easing curves/durations (FLIP-style reflow for scrollable views), GPU-accelerated (transform/opacity), fully respects reduced-motion.
Pinned-message cleanup — self-pin fades after ~10s (configurable), other-user pins auto-hidden client-side, smooth reflow (no layout jump), setting to disable.
Auto-update — resilient to failed/interrupted updates, falls back to last known-good version.
Disable-built-in-feature confirmation — **superseded 23 July 2026: owner asked that confirmation popups be replaced with a hold-to-confirm interaction project-wide.** UserPFP/UsrBG/GlobalBadges/RPC Bridge toggles now require holding the switch (~900ms) to turn off instead of a popup dialog; turning on stays a single click. See `src/renderer/ui.ts` `holdToggleRow()`.
Localization scaffolding — all UI strings externalized from the start, English-complete, fallback-to-English for missing strings, RTL-aware base layout — even if only English ships translated at launch.

Website — separate repo, build alongside V1

The project website lives in its own repository under the Hyaecord GitHub organisation: Hyaecord/website. It is deployed on Vercel at https://hyaecord.vercel.app (custom domain may come later — don't hardcode the vercel.app hostname anywhere that's painful to change).

Requirements:

Landing page — static, fast, no framework required. Presents the project (name, logo, tagline, feature highlights), links to the GitHub org/repo, downloads/releases, and the Discord server. Uses the branding assets from the main repo (assets/branding/), keeps to the brand palette already used in the README badges (cream #f1e9d5, red #e44550, amber #c88633), respects prefers-color-scheme and prefers-reduced-motion — the accessibility bar for the client applies to the website too.
/discord redirect — https://hyaecord.vercel.app/discord must redirect to the Discord server. The invite URL is NOT hardcoded: a Vercel serverless function fetches the server widget at request time (https://discord.com/api/guilds/1529521295228928000/widget.json, server ID 1529521295228928000) and 302-redirects to its instant_invite field, so the link keeps working if the widget invite rotates. Hardcode the last-known invite only as a fallback for when the widget fetch fails. Requires the server widget to stay enabled in Discord server settings — note this in the website README.
All links in the main repo (README badges, "join our Discord" copy) point at https://hyaecord.vercel.app/discord, never at a raw discord.gg invite, so the invite can rotate without touching the main repo.

V2 / Later — don't build yet

~~Theme Store~~ — **superseded 24 July 2026, owner asked for it early; a colours-only version shipped** (see the 24 July update near the end of this doc). Still genuinely V2/later: **Layouts and Catboxes** (including the "Discord Vanilla" catbox) — those aren't just colour palettes and need real design work — and a **formal moderation/review pipeline** (the shipped version relies on manual PR/issue review, which is fine at the current scale but won't stay fine forever).
Stat integrations beyond the first three (Steam, RetroAchievements, Last.fm) — build the generic "stat provider" framework, but validate it against these three before calling it done; Trakt/AniList/osu!/Strava/Chess.com etc. are backlog
Sponsorship nudge popups
Beta/canary release channel
Community translation contribution pipeline (Crowdin/Weblate-style) — the scaffolding is V1, the actual pipeline is V2
Credits screen with scroll/reveal animation
Section 27 ideas (custom notification sounds, DND scheduling, local search indexing, keybind customization UI, draft sync) — none of these are committed scope, revisit after V1 ships
A published plugin compatibility matrix / known-issues tracker for plugins that don't run cleanly under the new GUI — start the list in V1 as issues surface, but a polished public-facing version is V2

Explicitly Out of Scope

macOS support
Nintendo Switch client — if this ever happens, it's a separate, much smaller companion app (rich presence / lightweight chat only), not a port of Hyaecord itself. Native Electron/Chromium apps don't run on Switch homebrew at all.
Anything that automates actions, spams, or otherwise crosses from "cosmetic/QoL" into abusive-behavior territory — this is the one thing that meaningfully raises real ToS risk
Reusing Equicord's client codebase wholesale — the GUI and internals are being rebuilt; only the plugin API surface is intentionally kept compatible

Still Open — resolve before touching the relevant feature

[OPEN] Final product name — resolved: Hyaecord
[OPEN] Mascot name/visual design (logo being made separately by project owner) — hyena-themed is the obvious lane given the name, but not decided or built yet
[OPEN] VPN providers to support beyond Mullvad (ProtonVPN, NordVPN, WireGuard-based setups) — prioritize by overlap with the Discord-modding userbase
[OPEN] Theme Store moderation model at scale — the shipped version (24 July 2026) uses manual maintainer review of GitHub issues/PRs, which is fine now but revisit (community-voted vs. hybrid) once submission volume grows
[OPEN] Whether the self-pin auto-fade should have a brief "undo" window before disappearing
[OPEN] Flatpak app ID / .desktop command — the product name is now locked, which unblocks picking an ID (e.g. reverse-DNS style, something like org.hyaecord.Hyaecord, pending confirmation of who/what owns the domain or namespace this actually publishes under). Still open: the mullvad-exclude command syntax needs to be re-verified against whatever ID is finalized, since it's currently written against Equibop's — don't assume the syntax carries over 1:1 just because the ID pattern is similar.
[OPEN] Scope and format of the plugin compatibility/known-issues list — how it's tracked during V1 (issue labels vs. a doc) and what "supported" vs. "unsupported" means precisely, given the GUI is original rather than reused

⚠ Claims to verify before relying on in code

Exact Steam Web API (ISteamUserStats/GetPlayerAchievements) and RetroAchievements API request/response shapes
mullvad-exclude command syntax and how it adapts to Hyaecord's own Flatpak ID (currently written against Equibop's, and the ID itself isn't finalized yet — see above)
Discord's current upload-size recommendations for banners/icons/emoji — public sources on this disagree with each other as of writing (see item 6 above); go to primary sources, not aggregator blog posts
Current accuracy of "no observed ban pattern for client mod use" before it goes in any public-facing FAQ
Which specific Equicord/Vencord plugin API surfaces (patch points, context menus, message/component injection hooks) the original GUI needs to expose identically for compatibility to hold in practice — this needs to be enumerated against Hyaecord's actual component tree, not assumed from Equicord's

Reference: full original brief

The complete, unabbreviated project brief (all 27 sections plus research notes) is preserved separately — this document is the condensed, build-ordered version of it for Claude Code to work from. If Claude Code needs the full rationale/detail behind any V1 item, ask and the source section can be provided.

---

# BUILD LOG — state as of 22 July 2026

This section records what has actually been built so far and what the next agent should pick up. Everything below is real, on-disk state, not plans.

## Environment setup already done on this machine

- **GitHub CLI** installed at `~/.local/bin/gh` (v2.96.0) — the system git config previously pointed at a nonexistent `/usr/bin/gh`; `gh auth setup-git` has been re-run so the credential helper now points at `~/.local/bin/gh`. Authenticated as `aaronateataco` (scopes: gist, read:org, repo).
- **Node.js v22.17.0** installed to `~/.local/node-v22.17.0-linux-x64`, symlinked into `~/.local/bin` (`node`, `npm`, `npx`, `pnpm`).
- **Always `export PATH="$HOME/.local/bin:$PATH"`** before running node/pnpm/gh in this environment.
- `rsvg-convert` is available for SVG→PNG icon generation; ImageMagick is not installed.

## Repo 1: Hyaecord/hyaecord (main client) — pushed through commit "Tighten legal posture…"

**Committed and pushed:**
- README rewritten for accuracy: Hyaecord is described as an **original client with a plugin-API compatibility layer**, not a fork of Equicord (the old "fork of Equicord" wording contradicted this brief and weakened the legal position). Credits section reworded accordingly. Discord badge/link now points at `https://hyaecord.vercel.app/discord`; the `hyraecord.vercel.app` typo is fixed.
- `assets/branding/LICENSE.md` — NEW. Explicit all-rights-reserved carve-out for brand assets, closing the loophole where the repo-wide GPL LICENSE could be read as covering the logo.
- `TRADEMARK.md` — added a "Relationship to the GPL" note: the policy governs only the name/brand, never the code; rebranded forks need no permission. This pre-empts "your trademark policy violates the GPL" attacks.

**Written locally but NOT YET COMMITTED (do this first):**
- `package.json`, `tsconfig.json`, `.gitignore`, `pnpm-workspace.yaml` (contains `allowBuilds: electron/esbuild: true` — required by pnpm v11, the older `pnpm.onlyBuiltDependencies` field in package.json is ignored)
- `scripts/build.mjs` — esbuild bundler for main (cjs), preload (cjs), renderer (esm); copies HTML/CSS/i18n into `dist/`
- `src/shared/` — `types.ts` (settings, DE info, bridge interface), `constants.ts` (APP_ID, DEFAULT_SETTINGS, IPC channel names)
- `src/main/` — `index.ts` (single-instance lock, BrowserWindow with contextIsolation+sandbox, IPC handlers), `settings.ts` (atomic write-then-rename JSON store), `theme.ts` (XDG_CURRENT_DESKTOP → gnome/kde/other + nativeTheme), `i18n.ts` (locale merged over English fallback), `tray.ts` (branded tray icon + menu)
- `src/preload/index.ts` — contextBridge exposing the typed `window.hyaecord` API
- `src/renderer/` — `index.html` (four-pane shell with ARIA roles: server rail → channels → chat → members), `styles.css` (Light/Dark/AMOLED token sets, shared motion tokens, reduced-motion handling, text/UI scale variables), `app.ts` (loads settings/DE/locale, applies theme, renders placeholder shell)
- `src/i18n/en.json` — externalized strings incl. first-run wizard and disable-feature-confirmation copy
- `electron-builder.yml` — appId `org.hyaecord.Hyaecord`, AppImage/deb/rpm + nsis targets, desktop entry
- `assets/icons/` — `hyaecord-{16,32,64,128,256,512}.png` generated from the branding SVG via rsvg-convert, plus `hyaecord.svg`

**Verified working:** `pnpm install`, `pnpm typecheck` (clean), `pnpm build` (bundles successfully). Electron launches — note it needs `--no-sandbox` in this dev environment because the pnpm-store chrome-sandbox binary isn't setuid root; that is a local dev quirk, not a code bug, and packaged builds are unaffected.

**Next commit should be:** `git add -A && git commit -m "Scaffold Electron client: main/preload/renderer, tray, DE theme detection, settings store, i18n, icons, packaging config"` then push.

## Repo 2: Hyaecord/website (deployed at hyaecord.vercel.app)

**Committed, pushed, and verified live:**
- `index.html` — landing page
- `api/discord.js` + `vercel.json` rewrite — `/discord` fetches `https://discord.com/api/guilds/1529521295228928000/widget.json` at request time and 302s to `instant_invite`; falls back to the hardcoded last-known invite (`https://discord.com/invite/vhXNzJxV`) only if that fetch fails. Edge-cached 5 min. **Requires the Discord server widget to stay enabled.**
- `privacy.html` — no cookies/analytics; documents Vercel's transient hosting logs with a GDPR Art. 6(1)(f) legitimate-interest basis; GDPR/UK GDPR/CCPA rights honoured
- `legal.html` — non-affiliation with Discord Inc., nominative fair use of the "Discord" mark, ToS disclosure, GPL-vs-branding split, no-warranty notice, and a takedown/counter-notice section
- `docs.html` — docs/wiki hub with FAQ accordions
- `llms.txt` — machine-readable project summary so LLMs describe the project accurately
- `.well-known/security.txt` — RFC 9116 contact pointing at GitHub private security advisories
- `robots.txt`, `sitemap.xml`, `404.html`
- Security headers in `vercel.json` (CSP, HSTS, X-Frame-Options DENY, nosniff, referrer + permissions policy) — all confirmed present on the live response

**Written locally but NOT YET COMMITTED:**
- `index.html` **redesign** — sticky blurred nav bar, gradient-mesh animated hero with gradient-clipped headline text, pill buttons, 9-card feature grid, 6-tile download grid, honest fine-print notice, expanded footer. Visual direction is *inspired by* modern OSS client landing pages (Vencord's included) but written from scratch: own palette (cream/amber/red hyena tones, not Vencord's purple), own copy, own layout structure, no copied CSS/markup/assets. **Do not copy Vencord's stylesheet, artwork, mascot, or wording verbatim** — that is the line between inspiration and infringement.

## Repo 3: Hyaecord/hyaecord.wiki (GitHub wiki) — BLOCKED

Wiki pages are written and committed locally in `/home/aaron/Documents/Github/hyaecord.wiki/`: `Home.md`, `Installation.md`, `FAQ.md`, `Building-from-Source.md`, `Plugin-Development.md`, `Plugin-Compatibility.md`, `Roadmap.md`, `_Sidebar.md`.

`has_wiki` was flipped to `true` via the API, but **GitHub does not create the `.wiki.git` backend until the first page is created through the web UI**, so `git push` to `hyaecord.wiki.git` fails with "Repository not found".

**Manual unblock (owner action):** visit `https://github.com/Hyaecord/hyaecord/wiki`, click "Create the first page", save anything. Then:
```bash
cd /home/aaron/Documents/Github/hyaecord.wiki
git pull --rebase origin master   # or main, whichever GitHub created
git push origin master
```

## Update — 22 July 2026 (evening session)

Done since the log above was written:

1. ✅ Client scaffold committed and pushed (commit "Scaffold Electron client…").
2. ✅ CI (`.github/workflows/ci.yml`: typecheck + build) and release workflow (`.github/workflows/release.yml`: tag-driven electron-builder for Linux + Windows, draft release upload) written and committed. **Push blocked:** the gh OAuth token lacks the `workflow` scope, so GitHub rejects any push containing workflow files. Owner action: `gh auth refresh -h github.com -s workflow`, then `git push`.
3. ✅ `CONTRIBUTING.md`, `CODE_OF_CONDUCT.md` (Contributor Covenant 2.1), `SECURITY.md` (GitHub private advisories) committed (same blocked push).
4. ✅ Website landing page redesigned per owner direction: **no gradients anywhere, flat colours only, all colours sourced from the logo**, AA-checked contrast in both colour schemes. Palette extracted from the actual logo raster: amber `#c88633`, spot brown `#2e1d11`, sticker white `#fefefe`, shadow red `#e44550`, tongue pink `#d4495f`, cream `#f1e9d5`.
5. ✅ Branding kit added under `assets/branding/`: `BRANDING.md` (philosophy — flat/bold/sticker-like, no gradients; logo do/don'ts; palette table with contrast guidance), plus recolourable monochrome marks: `logo-mono.svg` (silhouette) and `logo-lineart.svg` (spots/features, red shadow excluded), both filled with `currentColor` so CSS `color` recolours them, plus black/white 1024px PNG renders. Owner rejected the first raster-masked versions, then also rejected the traced full-colour `logo-vector.svg` (auto-tracing posterizes the original's soft shading — a 48-colour/2048px re-trace was tried and still failed, at 3.5MB). **Final state: `logo.svg` (original) is the only full-colour master; use PNG renders of it for size-sensitive contexts. Only the mono/line-art marks are vector (`logo-mono.svg`, `logo-lineart.svg`).** Don't re-attempt full-colour tracing.

## Still to do

1. Owner: `gh auth refresh -h github.com -s workflow` then push the main repo (two local commits waiting).
2. Owner: initialize the wiki via the web UI ("Create the first page"), then push the eight prepared pages from `/home/aaron/Documents/Github/hyaecord.wiki/`.
3. ✅ (partially) First-run wizard + settings UI built (22 July 2026, evening): `src/renderer/ui.ts` (shared state, i18n with `{var}` substitution, focus trap, switch rows), `wizard.ts` (5 steps: theme with live preview + DE-detection copy, login import — **UI stub only, actual import logic not implemented**, built-in features, telemetry opt-out, ToS disclosure), `settings-ui.ts` (gear button in server rail; sections: appearance, accessibility incl. reduced-motion/text/UI scale, behaviour, integrations with the required disable-confirmation modal, privacy/telemetry). All controls apply live via `patchSettings`. Still missing from V1: Discord connection, plugin loader, actual login import, Gaming Mode detection, auto-update.
   Dev-environment gotcha discovered: this machine's agent shell exports `ELECTRON_RUN_AS_NODE=1` — unset it (`env -u ELECTRON_RUN_AS_NODE`) or Electron's `app` is undefined at launch.
4. ✅ Design tokens encoded into `styles.css`: OKLCH ramps + semantic tokens per theme from DESIGN.md (light-mode accent now amber-600 per contrast table; `--accent-strong`, `--danger-text`, `--danger-solid` added).
5. ✅ **Discord connection core started** (`src/main/discord/`): `token-store.ts` (safeStorage-encrypted, refuses plaintext persistence), `rest.ts` (minimal v10 REST), `gateway.ts` (v10 JSON gateway: hello/identify/heartbeat+jitter/ack-zombie-detection/resume/invalid-session/backoff reconnect; 4004 does not retry), `index.ts` session manager with auto-login. Renderer `session.ts`: token-paste login view (QR/import still to come), guild rail with real icons from CDN, text-channel list from READY, channel selection updates header/composer. ⚠ untested against a real account yet — the gateway payload shapes for *user* accounts (READY `guilds[].properties` vs legacy, channel ordering, capabilities flags) need live verification.
6. ✅ **Message view**: REST `getMessages`/`createMessage`, IPC + bridge methods, renderer renders channel history (avatar/author/time/content, textContent-only — no HTML injection path), live `MESSAGE_CREATE` appends with stick-to-bottom scroll, Enter in the composer sends (restores draft on failure). Same live-verification caveat as above; markdown rendering, message grouping, edits/deletes, and pagination are future increments.
7. ✅ **Website rebuilt on a Discord-structured neutral palette** (23 July 2026): the site's dark/light neutrals now mirror Discord's own surface scale (`#313338`/`#2b2d31`/`#1e1f22` dark, `#ffffff`/`#f2f3f5`/`#e3e5e8` light) instead of the earlier warm-brown/cream scheme — logo colours (amber/red/pink) are accents only, never a page canvas, per owner direction ("just like a discord dark/light theme but blurple replaced with our colours"). Added: dismissible pre-release banner, a real mobile hamburger nav (`script.js`, CSP updated to allow `script-src 'self'`), and an absurd pseudo-Latin pronunciation section (`#pronunciation`, linked from every page's nav) grounded in real etymology (Hyaenidae + chorda) per owner request. Verified responsive at 375px and 1280px via Electron screenshot capture, including the open mobile-menu state (caught and fixed a `flex-wrap` bug that broke the dropdown layout). Note: a same-day manual edit (commit `ac927d8`) shortened the hero subtext and cleared `FALLBACK_INVITE` in `api/discord.js` to `""` — that second change means `/discord` redirects to an empty URL if the widget fetch ever fails; flagging in case it wasn't intentional, not overridden.
8. ✅ **Hold-to-disable replaces confirmation popups** (23 July 2026, owner direction: "instead of confirmation popups anywhere, make it use a hold button instead"): `holdToggleRow()` in `src/renderer/ui.ts` — turning a built-in feature (UserPFP/UsrBG/GlobalBadges/RPC Bridge) on is a single click, turning off requires holding the switch ~900ms (a red fill tracks hold progress; releasing early cancels with no state change). Replaces the old `confirmDisable()` overlay dialog entirely. Apply this same pattern to any other future destructive/toggle-off action instead of a new popup.
9. ✅ **Native mention/DM notifications** (`src/main/notifications.ts` + `discord/index.ts` `maybeNotify()`): fires from the main process so it survives an unfocused/minimized window; only DMs and direct `@mention`s, suppressed if the window is already focused, click restores/focuses the window. This is the "minimal always-on listener for mentions/DMs/calls" half of Gaming Mode — the "detect another app went fullscreen" half remains unbuilt and is genuinely hard cross-desktop (see the honest gap noted in commit `6637edd`); the `gamingMode` setting is still an inert boolean.
10. ✅ **License changed** (23 July 2026, owner-provided text): the project moved from plain GPL-3.0 to the **Hyaecord Public License** — GPL-3.0-derived with two added conditions: no commercial use (no selling/paywalling the software, forks, or plugins; donations/tips remain fine) and forks/derivatives must stay open-source under the same terms. `LICENSE` itself was already updated by the owner; this pass propagated the change everywhere else that named GPL-3.0: `package.json` (`"license"` → `"SEE LICENSE IN LICENSE"`, since there's no SPDX identifier for a custom license), `README.md`, `TRADEMARK.md` (careful rewrite — the "forks never need permission, only rebranding" guarantee now explicitly notes it's conditioned on staying compliant with the *source* license's own non-commercial/copyleft terms, not just the branding policy), `CONTRIBUTING.md`, `assets/branding/LICENSE.md`, and every mention across the website (`llms.txt`, `legal.html`, `docs.html` FAQ, all page footers). Nothing should say "GPL-3.0" unqualified anywhere in either repo anymore — always "Hyaecord Public License (GPL-3.0-derived)".
11. ✅ **Client light theme no longer uses cream** (owner: "dont theme the client at all around that offwhite cream... it is mainly just for the website"): `[data-theme="light"]` in `src/renderer/styles.css` now uses the same true-neutral-grey scale as the website's Discord-modeled light theme (`#ffffff`/`#f2f3f5`/`#e3e5e8`, text `#060607`) instead of the earlier cream-tinted near-white. DESIGN.md's token table and contrast notes updated to match, including an honest callout that near-black-on-white breaks the doc's own "avoid pure black/white, it causes halation" guidance — kept anyway because matching Discord's actual light theme was the explicit ask, not a hypothetical general audience.
12. ✅ **Website spacing/layout pass** (owner: "make sure the website just isn't clunky... some sections shouldn't take up that much space also pronunciation should be at the bottom maybe"): section vertical padding 3.2rem → 2.4rem, card padding 1.4rem → 1.1rem/1.2rem, grid gaps tightened. The pronunciation bit moved from its own full section near the top to a collapsed `<details>` footnote at the very bottom (after the legal notice, before the footer) — closed by default so it costs zero space unless opened; the nav's "Pronunciation" link and `script.js` now auto-expand it via `openDetailsFromHash()` when linked to directly, since a native `<details>` doesn't open itself just because the page scrolled to it.

## Snippet-inspired features — designed, not yet built

The owner supplied a batch of React/Next.js UI-library snippets (kokonutui, React Bits) as *design references* on 23 July 2026 (`uisnippets.txt`, since consumed — content preserved here). **This project has no React, GSAP, Three.js, or Motion dependency and shouldn't gain one for cosmetic effects** — the client is vanilla TS/Electron and the website is static HTML; each item below is translated to a vanilla CSS/TS approach that reproduces the same *interaction*, not the framework code. Pick one per loop iteration; update this list as each ships.

- **Avatar/profile-setup picker** — first-run/profile UI: stage + thumbnail strip, selected thumbnail gets a coloured ring and a checkmark badge, cross-fade on selection. Vanilla port: CSS `transition` on `box-shadow`/`opacity`, no spring physics needed for something this small.
- **Contributor badge animations** — for the Hyaecord Contributor badge / plugin contributor lists: staggered fade+slide-in per avatar (CSS `animation-delay` per `nth-child`), small "shake" on invalid interaction (a `@keyframes` translateX wiggle), spring-like avatar-count add/remove — a CSS `transition: transform cubic-bezier(...)` on scale approximates the spring well enough without a physics lib.
- **Dynamic rotating text** — ✅ shipped (24 July 2026): `mountRotatingText()` in `src/renderer/ui.ts`, used for the chat header while `connecting`/`reconnecting` (`CONNECTING_KEYS` in `session.ts`, 2.8s cadence, loops continuously since a slow handshake has no natural end point unlike the reference's one-shot greeting demo).
- **Particle burst on donate/support click** — ✅ shipped, but retargeted: there's no real donation URL configured yet (owner hasn't provided one, and fabricating a financial destination isn't something to guess at), so the burst fires on a **"Star on GitHub"** button in Settings → Support instead — same positive-feedback interaction, zero financial/trust risk. `burstParticles()` in `ui.ts`; opens externally via a new `hyaecord:open-external` IPC handler restricted to `https://` URLs. Revisit and point at a real Ko-fi/GitHub Sponsors link once the owner sets one up.
- **Moderator View — mass-delete channels (Shift+M)** — ✅ shipped 24 July 2026, see item 15 below.
- **Hold-button confirmations** — ✅ shipped for built-in feature toggles (`holdToggleRow`, item 8 above). Extend the same pattern to any future destructive action (message delete, leave server, moderator mass-delete) instead of adding new popups.
- **Social/connections display on profiles** — ✅ shipped (23 July 2026) as part of the profile popout itself, see item 25 below. Plain labelled rows for now, not the hover-expanding icon-button treatment from the reference — revisit the interaction polish later if it's worth it.
- **Channel sidebar "proximity" hover effect** — ✅ shipped (24 July 2026): `wireChannelProximity()` in `session.ts`, a `pointermove` handler over `#channels` setting a `--effect` custom property per `<li>` (distance-based, 90px radius), consumed in CSS via `color-mix()` and a small `translateX`. No rAF loop needed at this list size — the CSS transition alone smooths it.
- **Theme picker as a searchable gallery** — the Theme Store shipped 24 July 2026 as a simple grid (see item 17 below); revisit a search/filter/carousel UI once the registry has enough entries that scrolling a grid stops being enough.
- **Website card-style mega nav** — the current sticky nav + mobile hamburger (shipped item 7) covers V1 needs; the reference `CardNav` (expanding multi-column card menu) is a nice-to-have for once the site has enough sub-pages to justify it. Skip for now.
- **"Server Chomper" — swipeable hide/mute for servers** — ✅ shipped 24 July 2026 for guilds, see item 16 below. DM hide/mute is still open (needs a different, unverified mute endpoint).
- **Masonry GIF picker** — for the GIF-search picker (not yet built at all — no GIF search integration exists). Vanilla masonry via CSS `columns` or a simple JS column-balancing layout is sufficient; skip GSAP.
- **Server folders** — ✅ shipped (23 July 2026), see item 24 below. Local-to-this-client only, not synced to Discord's real protobuf folder settings.
- **Lanyard-style contributor profile effect** — the reference is a full physics-simulated 3D swinging ID badge (Three.js + Rapier). **Recommend against porting this literally** — it would be the only 3D/WebGL dependency in an otherwise lightweight app, for a cosmetic profile flourish. If pursued, prefer a lightweight CSS `perspective`/`rotateX/Y` tilt-on-hover for the Hyaecord Contributor badge instead of true physics.
- **Integer slider for numeric plugin settings** (e.g. a "schedule message" delay) — belongs to the plugin settings UI, which doesn't exist yet (no plugin loader is built). Build a plain `<input type="range">` row (see `scaleRow` in `settings-ui.ts` for the existing pattern) once plugin settings schemas exist.

## Update — 24 July 2026 (major groundwork + feature session)

Owner direction: stop deferring the blocked backlog items — build the prerequisite data models and ship as many of the blocked features as those actually unblock, in one continuous pass, no scheduled pauses between increments. Also: de-brand the default client themes (amber is for the logo/website, not the everyday app UI), and build a real Theme Store now rather than waiting for V2, with zero hosting cost (GitHub-hosted registry, no backend). Each item below is its own commit; check `git log` for exact diffs.

13. ✅ **Default themes de-branded**: `--accent`/`--accent-strong` in Light/Dark/AMOLED no longer point at the brand amber ramp — they now use a neutral teal (`#2dd4bf` dark/AMOLED, `#115e59` light), deliberately distinct from both the brand identity *and* Discord's blurple. Dark/AMOLED `--text`/`--text-dim` also lost their last cream tint (now `#f2f3f5`/`#949ba4`, matching the light theme's earlier de-creaming). Brand amber/cream tokens (`--brand-amber`, `--amber-*`, `--brand-cream`) are untouched and still defined — reserved for a possible future built-in "Hyaecord" branded theme, just not wired to any *default* theme. DESIGN.md §2.3/§2.4/§2.5/§8/§9 updated to match, with contrast recomputed (not guessed) for every new pairing.
14. ✅ **Groundwork: DM list + real permission computation.** `src/renderer/permissions.ts` implements Discord's actual permission-resolution algorithm (role base → ADMINISTRATOR short-circuit → @everyone channel overwrite → role overwrites → member overwrite) using `BigInt` bitfields. `session.ts` now parses `private_channels` from READY into a DM list behind a new 💬 rail pill, and computes `canManageChannels` per guild from the same READY payload's `roles`/`member` data. **This did *not* need new IPC** — dispatch events were already forwarded to the renderer raw, so it's parsing logic, not new plumbing. Same standing caveat as always: the exact shape of role/member data in a *user*-account gateway READY is unverified against a real session — the code is defensive on purpose (missing data resolves to "no permission", never a guessed grant), so gated features fail closed, not open.
15. ✅ **Moderator View built** (Shift+M in a server you can manage channels in — gated on item 14's real permission computation, not a guess): multi-select channels, hold 1.2s on "Hold to feed to the den" to confirm (the established hold-instead-of-popup pattern, `holdButton()` — a new standalone sibling to `holdToggleRow`), selected channels play a "chomp" animation before their `DELETE /channels/{id}` calls fire. `Escape` exits without deleting anything. New REST/IPC path: `rest.ts` `deleteChannel()` → `discord/index.ts` → `hyaecord:discord-delete-channel` → bridge `deleteChannel()`.
16. ✅ **Server Chomper built**: pointer-drag a server icon horizontally (>70px) to hide it from the rail and mute it server-side (`PATCH /users/@me/guilds/{id}/settings`, new `setGuildMuted()`/`muteGuild()`). Settings → Server Chomper shows the hidden count and a toggle — **flipping it on shows the hidden items again and un-mutes them; flipping it back off re-hides and re-mutes the same set** (matches the owner's spec exactly: a real two-way toggle, not a one-shot "restore" button). Initially scoped to guilds only, since DM muting needed a different, unverified endpoint — extended to DMs in item 21 below once that endpoint was actually looked up.
17. ✅ **Theme Store built, zero hosting cost.** `community-themes/registry.json` in this repo is the entire "backend" — served free via `raw.githubusercontent.com`, no server, no database. A community theme is **only ten hex colour values** (`community-themes/README.md` documents the schema and the WCAG contrast bar), never CSS or code — applied as inline custom-property overrides on top of whichever base theme (Light/Dark/AMOLED) is active, so there is no code-injection surface at all. Submission is two zero-cost paths: a structured GitHub issue template (`.github/ISSUE_TEMPLATE/theme_submission.yml`) or a PR directly against `registry.json`; the in-client picker (Settings → Appearance → Browse themes) links to both, plus the Discord server for anyone who'd rather not use GitHub. Two seed themes shipped (Midnight Ocean, Evergreen), both contrast-verified. `src/main/community-themes.ts` fetches + validates the registry from the *main* process (no renderer CSP changes needed, consistent with how telemetry/notifications already work) and defensively drops any entry missing a required token or with a malformed hex value.
    **Verification note:** confirmed the file is live via `gh api repos/Hyaecord/hyaecord/contents/...` immediately after push; the public `raw.githubusercontent.com` URL 404'd for about a minute afterward, which is normal Fastly edge-cache propagation for a brand-new path, not a bug — don't re-chase this if it happens again after a fresh push.
18. ✅ **Real Discord login (email/password), not just a token paste** (owner: token-only login "kinda sus"). `src/main/discord/rest.ts` gained unauthenticated `loginWithCredentials()`/`submitMfaTotp()` hitting the same `/auth/login` + `/auth/mfa/totp` endpoints the official client uses. `discord/index.ts` wraps these as `loginWithCredentials()`/`submitMfa()`, both funneling into the existing `completeLogin()` (gateway start + token persistence) on success — token-based `login()` didn't change, it's just no longer the *only* door in. Handles authenticator-app (TOTP) 2FA; **SMS/backup-code MFA and hCaptcha challenges are not implemented** — both surface as a clear, specific error message rather than hanging or failing silently.
    **Real-account test result:** owner tried this and got "That code wasn't accepted" on the MFA step — first live signal we've had all session that a Discord integration piece might have an actual bug (vs. the standing "unverified" disclaimer). Not chased further because item 19 replaced this as the *default* path before diagnosing it; if the credentials+TOTP screen is still used later and this recurs, check the exact request body Discord's `/auth/mfa/totp` expects (field names, whether `login_source`/`gift_code_sku_id` nulls are actually required) against a packet capture of the real client, rather than assuming it's just a mistyped code.
19. ✅ **Real embedded-browser login, now the *default* path** (owner: the custom credentials form still "kinda sus", wanted the real discord.com page so autofill/trust signals are genuine). `src/main/discord/browser-login.ts` opens a plain `BrowserWindow` — no injected script, no modified page — pointed straight at `https://discord.com/login`, on its own persistent session partition (`persist:discord-web-login`, separate from Hyaecord's own session). The token is captured by watching that window's *own* network requests for the `Authorization` header Discord's web client sends once logged in (`session.webRequest.onBeforeSendHeaders`) — an observation at the Electron layer that never touches or modifies the page itself. Closing the window without completing resolves as `cancelled`, not an error. A first attempt got stuck at the login page — traced to Electron's default user agent identifying itself as Electron, which Discord/Cloudflare's bot detection routinely challenges; fixed by setting a plain desktop Chrome UA on that window (`browser-login.ts`).
    A **QR-code login was also built and then removed**: Discord's real login page already has its own QR-scan toggle built in, so a separate `remote-auth.ts` reimplementing Discord's QR protocol from scratch was pure redundant surface for no benefit over the real page — deleted along with the now-unused `qrcode` dependency. If QR login is ever wanted as a distinct entry point again (not just "toggle within the browser popup"), reimplementing it is a known-solvable problem — Discord's remote-auth protocol (`wss://remote-auth-gateway.discord.gg/?v=2`, RSA-OAEP keypair via Node's built-in `crypto`) was working code before it was removed; check prior commits if picking this back up.
    **⚠ Real-world incident, 23–24 July 2026:** the owner tested this live. Shortly after logging in through the browser flow and sending one message, Discord force-logged the account out on every device and required a password reset. This is almost certainly Discord's automated abuse-detection systems reacting to a new client fingerprint authenticating and immediately sending a message — a pattern that looks identical to self-bot/automation abuse from Discord's side, regardless of how the login happened. **This is not a bug to fix; it is the real, materialized version of the ToS risk this project has disclosed from day one.** The incident got substantially worse because the account's recovery email was tied to a school domain that had since been permanently retired (institution-side, no forwarding set up) — an unrelated, unlucky pre-existing condition that turned a recoverable lockout into a genuinely difficult account-recovery situation, still unresolved as of this writing. No code fix addresses the root cause here; the risk is inherent to what client mods are. Two things worth carrying forward if this integration keeps being developed against real accounts:
    - Consider whether the client should show a first-login caution (e.g., "avoid sending messages immediately after connecting for the first time") — a soft mitigation, not a guarantee, since the detection logic is entirely on Discord's side and unknown to us.
    - Prefer test/throwaway accounts over primary accounts for any future live verification of the Discord integration pieces still marked unverified in this document.
20. ✅ **First-login caution notice built** (the suggestion from item 19, done same session): a one-time dismissible notice appears above the message list the first time a session reaches `ready` via an explicit login this launch (not a restored session) — *"You just connected. Discord's systems can sometimes flag a brand-new login that sends a message right away…"*. Threaded through as a `freshLogin` flag on the `DiscordSession` state object, set `true` right before `completeLogin()` in every explicit login path (`login`, `loginWithCredentials`, `submitMfa`, `loginWithBrowser`) and left `false` for `autoLogin()`'s stored-token restore. Soft mitigation only, as noted above — it can't know what actually trips Discord's detection.
21. ✅ **DM muting for Server Chomper (closes item 16's gap)**, done properly this time: looked up the actual endpoint in [docs.discord.food](https://docs.discord.food/resources/user-settings) (the community-maintained documentation of Discord's undocumented user API, same source class the login endpoints already relied on) rather than guessing while the owner's own account was mid-lockout. DMs/group-DMs are muted via the same `channel_overrides` mechanism used for per-channel guild mutes, addressed through `PATCH /users/@me/guilds/@me/settings` — the literal string `@me` as `guild.id` scopes it to private-channel settings. New `RestClient.setDmMuted()` / `discord/index.ts` `muteDm()` / IPC `discordMuteDm`. Chomper's data model changed from `hiddenGuildIds: string[]` to `hidden: Array<{ id, type: "guild" | "dm" }>` to track both kinds under one restore toggle, matching the original design intent now that it covers more than guilds. Swipe-to-hide now works on DM list rows the same way it does on server pills (same `wireChomperDrag`, generalized). ⚠ Same as always — verified against documentation, not a live account.

22. ✅ **Gaming Mode's actual fullscreen detection (closes item 9's gap)**, built event-driven rather than polled: `xprop -root -spy _NET_ACTIVE_WINDOW` blocks and emits a line only when the active window changes, then a one-shot `xprop -id <id> _NET_WM_STATE` check looks for `_NET_WM_STATE_FULLSCREEN` on that window. Scoped honestly to X11/XWayland — there's no equivalent portable mechanism on native Wayland, so on anything else (or if `xprop` isn't installed) the feature reports itself as unavailable instead of silently doing nothing or crashing. Wired to the existing `gamingMode` setting: starts/stops the watcher on toggle and at launch, cleaned up on `before-quit`. New `GamingModeState { active, available }` pushed to the renderer over a new `gamingModeState` IPC channel, surfaced as a live status line under the Settings toggle (starting/unavailable/watching/active). Verified both paths live: normal typecheck/build/smoke test, plus a targeted screenshot test with the toggle switched on in this xprop-less sandbox, confirming the honest "Can't detect fullscreen apps on this system (needs X11 or XWayland)" message rather than a false positive — and confirmed no zombie `xprop`/electron processes were left behind after quitting.

23. ✅ **SMS and backup-code MFA for login (closes item 18's remaining gap)**, verified against [docs.discord.food](https://docs.discord.food/authentication) rather than guessed: all three account MFA methods share one endpoint shape, `POST /auth/mfa/{totp,sms,backup}` with `{code, ticket}` — backup codes go through the same call shape as an authenticator code, just a different code format — plus `POST /auth/mfa/sms/send` to have Discord text the code first. `loginWithCredentials` now reports every method the account actually offers (`res.totp`/`res.sms`/`res.backup` flags from the initial `/auth/login` response) instead of only recognising TOTP and refusing the rest. The login MFA screen gained a method picker (only shown when an account has more than one method) and an SMS-specific "text me a code" step ahead of the same code field. hCaptcha remains the one unhandled case, still surfaced as a clear error rather than failing silently. ⚠ Same as always — verified against documentation, not a live account (still logged out while the incident case is open).

24. ✅ **Server folders for the rail** (parity item from the V2/Later backlog, previously unstarted): drag one server pill onto another — a vertical gesture, deliberately orthogonal to Server Chomper's horizontal swipe-to-hide so a single pointer sequence commits to whichever axis actually moves first — to group them into a collapsible folder with a 2×2 mini-icon grid when collapsed, a double-click-to-rename label, and a random accent colour from a small fixed palette when expanded. Kept local-to-this-client deliberately: looked up the real storage shape on [docs.discord.food](https://docs.discord.food/resources/user-settings-proto) and confirmed Discord's actual account folders live in the "Preloaded User Settings" *protobuf* (`guild_folders` field), not the legacy JSON settings endpoint the rest of this app talks to — round-tripping that format wasn't worth the risk of a malformed write touching a real account's live settings for a cosmetic organizing feature, especially unverified live. An explicit "Remove from folder" button on each member pill covers keyboard/touch users, since the drag-to-group gesture itself has no non-pointer equivalent (Discord's own client has the same gap).

25. ✅ **Profile popout** (the prerequisite several backlog items were blocked on): click a message author's name or avatar to open a card showing their real avatar, banner/accent colour, pronouns, bio, Discord's own profile badges, and connected accounts — all from `GET /users/{id}/profile`, the exact endpoint the official client hits from the same click, verified on [docs.discord.food](https://docs.discord.food/resources/user) rather than guessed. Positions itself near the click and flips above it if it would run off the bottom of the viewport; closes on outside click or Escape. Deliberately doesn't fetch mutual guilds/friends (`with_mutual_guilds=false` etc.) since nothing in the app surfaces them yet — no point paying for data with nowhere to show it. This unblocks the connections display (done, see above) and gives badge rendering somewhere to live; it does not yet pull in GlobalBadges/Hyaecord-Contributor badges, since fetching that contributor list is still its own unbuilt piece — only Discord's own returned profile badges render right now.

26. ✅ **GlobalBadges wired into the profile popout**: fetches `https://badges.vencord.dev/badges.json` — confirmed live and working by curling it directly rather than trusting a secondary source, keyed by Discord user ID, cached in memory for 30 minutes so opening several profiles doesn't refetch the whole feed each time. Gated by the existing `settings.integrations.globalBadges` toggle on the main-process side, so a disabled integration makes zero network requests. The Hyaecord Contributor badge itself (`src/shared/badges.ts`) is still just a data definition — it needs a maintained mapping from GitHub contributor to Discord user ID that doesn't exist anywhere in the repo yet, and that's a data-curation call for the project owner, not something to invent.

27. ✅ **Masonry GIF picker** (closes the last item that was purely blocked on "GIF search integration doesn't exist"): a new GIF button next to the composer opens a search box over a plain CSS-`columns` masonry grid of autoplaying preview videos. Backed by Discord's own `GET /gifs/search` and `GET /gifs/trending-gifs` — verified on [docs.discord.food](https://docs.discord.food/resources/integration): both are proxied through Discord's API using the user's own token, no separate Tenor API key needed, contrary to what a naive "just use the Tenor API" approach would have assumed. Clicking a tile sends its URL as the message content, matching how the official picker behaves (Discord auto-embeds a bare GIF URL). Needed a CSP change (`media-src https:` added to `index.html`) since preview videos are served from Tenor's CDN, not this app's own origin — img-src already allowed `https:` for CDN avatars/icons but there was no equivalent for `<video>`.

28. ✅ **Avatar picker in Settings**: a new "Account" section (only shown when logged in) previews the current avatar and lets you upload a replacement (PNG/JPEG/GIF, 8MB client-side cap ahead of whatever Discord's own server-side limit turns out to be) or reset to the default. Backed by `PATCH /users/@me` with the avatar as a `data:image/...;base64,...` URI — verified against docs.discord.food's user endpoint *and* its CDN-data-format reference page rather than guessing at the encoding. On success the main process updates its cached user object and re-sends session state immediately, so Settings and the header reflect the new avatar without waiting on a gateway `USER_UPDATE` (which isn't wired up to anything yet — see the open member-list gap, same underlying gateway-event-handling limitation).

29. ✅ **Real member list** (closes a V1-scope gap — the fourth rail column, `#member-list`, had been an empty placeholder element since the shell was first built): wires Discord's gateway "lazy guild loading" protocol. Selecting a channel sends OP 14 (unofficially "Guild Subscriptions"/"Lazy Request") for the first 100 entries; `GUILD_MEMBER_LIST_UPDATE` dispatches with SYNC/INSERT/UPDATE/DELETE ops keep it live. Verified against the community lazy-guilds write-up rather than guessed, and one real documentation gap is called out rather than papered over: the event's `id` field is a permission-derived list hash, not a channel id, and nothing documented explains how to map it back to a specific subscription. Rather than invent that mapping, incoming updates are matched on `guild_id` only — correct as long as the app holds at most one member-list subscription open at a time, which is true today (selecting a channel always replaces the previous subscription) but would need real correlation if concurrent per-channel subscriptions are ever added. Clicking a member opens the existing profile popout (item 25) — the two features compose for free. Presence status dots (online/idle/dnd/offline) and role-based group headers (using role names/colours already parsed from READY) round it out.

## Still open after this session

- Native Wayland compositors still can't be supported for Gaming Mode's fullscreen detection (item 22) — there's no portable protocol for it; X11/XWayland only for now.
- hCaptcha solving for login (item 18/23) — still shows a clear "not supported yet" error rather than failing silently.
- Server folders (item 24) are local-only — they don't read or write Discord's real protobuf `guild_folders` settings, so they won't show up in the official client or sync across devices. Round-tripping that proto format is future work if it's ever worth the risk.
- The Hyaecord Contributor badge (item 26's remaining gap) needs a GitHub-login → Discord-ID mapping that doesn't exist yet — a data-curation decision for the project owner. Contributor badge animations and the lanyard-style tilt effect both wait on that specific badge existing.
- Member list — ✅ shipped (23 July 2026), see item 29 below. Only the first 100 entries per channel (no further paging), and correlates events to subscriptions by guild_id rather than a verified channel-level id — see the note in `member-list.ts` for why, and revisit if the app ever needs more than one concurrent subscription.
- Plugin numeric sliders remain blocked on the plugin settings UI, which doesn't exist yet (no plugin loader is built).
- Mascot/logo design work remains with the project owner (still [OPEN] above).
